apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
  namespace: limitrange
spec:
  containers:
  - image: nginx
    name: nginx
    resources:
      limits:
        memory: 250Mi
      requests:
        memory: 250Mi
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
