apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: nginx
    version: v1
  name: nginx-old
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
      version: v1
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: nginx
        version: v1
    spec:
      containers:
      - image: nginx:1.23.0
        name: nginx
        ports:
        - containerPort: 80
        resources: {}
        volumeMounts:
        - name: workdir
          mountPath: '/usr/share/nginx/html'
      initContainers:
      - image: busybox
        name: busybox
        command: ['/bin/sh', '-c', 'echo "Old deployment" > /work-dir/index.html']
        volumeMounts:
        - name: workdir
          mountPath: '/work-dir'
      volumes:
      - name: workdir
        emptyDir: {}

